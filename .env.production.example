# ============================================
# VARIABLES DE ENTORNO PARA PRODUCCIÓN
# ============================================
# ⚠️ IMPORTANTE: 
# 1. Copia este archivo a .env.production
# 2. Reemplaza TODOS los valores con datos reales
# 3. NUNCA subas .env.production a Git
# ============================================

# ============================================
# BASE DE DATOS (OBLIGATORIO)
# ============================================
# PostgreSQL es OBLIGATORIO en producción
# SQLite solo para desarrollo local

# Ejemplos de connection strings:
# Neon:      postgresql://user:pass@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# Supabase:  postgresql://postgres:pass@db.xxx.supabase.co:5432/postgres
# Railway:   postgresql://user:pass@containers-us-west-xxx.railway.app:5432/railway
# Replit:    Se configura automáticamente desde el panel

DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_base_datos

# ============================================
# SERVIDOR
# ============================================
PORT=5000
NODE_ENV=production

# ============================================
# SEGURIDAD (OBLIGATORIO)
# ============================================
# GENERA UNA CLAVE ALEATORIA SEGURA (mínimo 32 caracteres)
# Comando en PowerShell para generar:
# -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | ForEach-Object {[char]$_})
# O en Node.js:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

SESSION_SECRET=CAMBIA_ESTO_POR_UNA_CLAVE_ALEATORIA_MUY_LARGA_Y_SEGURA

# ============================================
# AUTENTICACIÓN REPLIT (Opcional)
# ============================================
# Solo necesario si despliegas en Replit.com
# Replit configura esto automáticamente

REPL_ID=
ISSUER_URL=https://replit.com/oidc

# ============================================
# OPTIMIZACIONES (Opcional)
# ============================================
# Límite de conexiones al pool de PostgreSQL
# DB_POOL_MAX=20

# Tiempo de timeout para conexiones (ms)
# DB_CONNECTION_TIMEOUT=2000

# ============================================
# CORS (Opcional - si frontend está en otro dominio)
# ============================================
# CORS_ORIGIN=https://tu-dominio.com

# ============================================
# LOGS Y MONITOREO (Opcional)
# ============================================
# SENTRY_DSN=
# LOG_LEVEL=info

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Nunca uses valores por defecto en producción
# 2. SESSION_SECRET debe ser diferente en cada entorno
# 3. Asegúrate que DATABASE_URL usa SSL (?sslmode=require)
# 4. Verifica que todas las URLs usen HTTPS en producción
# 5. Mantén este archivo seguro y no lo compartas
